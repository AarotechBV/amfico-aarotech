# admin-pulse-shared

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-pulse-shared` to build the library.

## Running unit tests

Run `nx test admin-pulse-shared` to execute the unit tests via [Jest](https://jestjs.io).
