# amfitech-shared

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build amfitech-shared` to build the library.

## Running unit tests

Run `nx test amfitech-shared` to execute the unit tests via [Jest](https://jestjs.io).
